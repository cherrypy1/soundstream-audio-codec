def take_params():
    return {
        "seed": 67,
        "train_root": "data/librispeech/LibriSpeech/train-clean-100",
        "test_root": "data/librispeech/LibriSpeech/test-clean",
        "val_percentage": 0.05,
        "sample_rate": 16000,
        "crop_seconds": 0.5,
        "batch_size": 12,
        "num_workers": 4,
        "channels": 64,
        "latent_dim": 128,
        "codebook_size": 1024,
        "num_quantizers": 8,
        "model_lr": 3e-4,
        "discrim_lr": 3e-4,
        "total_steps": 45000,
        "one_batch_steps": 1000,
        "log_every": 50,
        "val_every": 500,
        "nisqa_every": 4500,
        "nisqa_batches": 3,
        "save_every": 4000,
        "checkpoint_dir": "checkpoints",
        "eval_dir": "outputs/eval",
    }
